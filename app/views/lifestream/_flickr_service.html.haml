/ begin conditional content
%li.item.flickr{:class => "#{last ? 'last' : nil}"}
  %p.site_info{:style => "background: white url(../images/flickr_favicon.ico) no-repeat scroll 3px center; -moz-background-clip: -moz-initial; -moz-background-origin: -moz-initial; -moz-background-inline-policy: -moz-initial;"}
    == Eu #{post.service_action} no 
    = link_to post.service.name, lifestream_path(post.service)
  .item_inner
    / domain-specific boxes
    %p.activity_image_text
      = link_to post.title, post.url
      - unless post.summary.blank?
        %span.activity_image_content= post.summary
    = link_to '', post.url, :class => 'activity_image', :style => "background: transparent url(#{post.extra_content['image_url_medium']}) no-repeat scroll center center; -moz-background-clip: -moz-initial; -moz-background-origin: -moz-initial; -moz-background-inline-policy: -moz-initial;"
  %p.date
    = relative_published_date(post.published_at)
    |
    = link_to 'Permalink', post.url
    = link_to('Admin', '#', :class => 'admin') if true